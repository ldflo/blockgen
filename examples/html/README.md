# Example: CSV to HTML table

This example demonstrates how to use `blockgen` to generate an HTML table from a CSV file.

Launching [`script.py`](script.py) will read [`data.csv`](data.csv) and inject the table headers and rows into [`preview.html`](preview.html).
